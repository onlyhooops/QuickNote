# Web 快速笔记应用 — 需求与技术方案（重构版）

> 状态：v2 需求已确认并实现骨架，UI 细节待迭代打磨

## 1. 定位

极简「随手记 + 时光轴」。用户在**第一屏**快速记录当下（纯文本/富文本 + 图文混排），保存后化作**时间点**构筑图形化**时间轴**；另有随览、回响等回顾入口。无账号、无权限、无多端同步。

## 2. 运行环境

| 项 | 值 |
|---|---|
| 开发环境 | macOS x86 |
| 运行环境 | Linux（systemd 服务，开机自启） |
| 访问方式 | 默认仅监听 127.0.0.1；可开放局域网供手机/平板（无鉴权，风险自知） |
| 移动端 | 响应式 + 底部三 Tab + PWA（可添加到主屏幕） |
| 部署形态 | 提供 systemd unit 文件，崩溃自动拉起 |

## 3. 技术选型

- 前端：Vue 3 + Vite；contenteditable 富文本编辑器（无第三方编辑器内核）
- 后端：Node.js + Express + 内置 `node:sqlite`
- 农历/节日：`lunar-javascript`（公历节日、农历节日、节气）
- 净化：DOMPurify（存储/渲染前统一净化）

## 4. 核心功能（v2 定稿）

### 4.1 首屏即录入（Write = 右侧工作区）
- 实时时间头：时:分 大字 + 日期、星期、公历节日、农历日期/节日、节气
- **单栏所见即所得编辑器（Tiptap）+ Lucide 图标**：工具栏颜色走主站 CSS 变量（明暗一致）；标题/加粗/斜体/删除线/列表/引用/链接/图片 + 附件（任意文件）；图片/附件上传到本地 `/api/images`、`/api/attachments`
- 显式「保存」：无内容（无文字且无图）不落库并轻提示；保存后清空、轻提示「已化作时间点」；⌘/Ctrl+Enter 快速保存

### 4.2 时间点与时间轴（Timeline）
- 每条记录 = 一个时间点；时间轴按**创建时刻**排序，最新在上、纵向居中单线、按日期分组
- 图形化呈现：不铺内容，仅时间点圆点与时刻；标签做小徽标
- 桌面悬浮圆点 → 内容浮卡预览（自动避让视口边缘，淡出遮罩）；点击 → 全屏查看/编辑
- 移动端：点按直达全屏（无 hover），时间轴左线布局
- 时间轴顶栏：关键字检索（服务端纯文本子串匹配）+ 标签过滤胶囊 + 计数
- 全屏页：查看态（优雅大字排版）⇄ 编辑态（富文本 + 标签）；编辑不改动时间点位置；可删除（确认）

### 4.3 随览（Random）
- 侧栏「回顾」分组（移动端顶部图标）打开浮层：从全部时间点随机取一条，大字号排版展示，可「再拾一条」，可进入详情；空库引导去录入

### 4.4 回响（Historical echo）
- 侧栏「回顾」分组（移动端顶部图标）打开浮层：展示**所有年份**中「同月今日」时刻的记录，按年份分组（新在前），同 ±2 小时标注「约今时」
- 无记录时随机留白文案（多套备选），并提示去记录今天

### 4.5 主题
- 浅色 / 深色 / 跟随系统三选；设置页选择，顶栏图标循环切换；localStorage 记忆 + 首帧防闪脚本 + meta theme-color 同步

### 4.6 标签（简化）
- 仅在编辑态点选：输入回车添加 / 快捷预设（灵感/备忘/摘抄/随笔）/ 点✕移除；时间轴仅做筛选展示

### 4.7 数据模型
- notes：`content`（净化后 HTML）+ `plain`（纯文本，检索/标题/摘要用）+ `created_at` / `updated_at`
- 标签：tags + note_tags（同 v1）
- 图片：独立文件于 `server/data/images`，Markdown 中以 `/images/xxx` 引用
- 前端存储前统一经 DOMPurify 净化；渲染直接用净化 HTML

### 4.8 备份（沿用 v1）
- 单向 WebDAV（本地→云端 zip：DB + 图片）；手动 + 定时自动；本地保留最近 5 份；凭据存 `server/data/config.json`（明文，本机可接受）
- 「设置」页承载：外观 + 备份配置 + 说明

### 4.9 卡片智能排版（9 的落地）
- 依据内容自动分级排版：无图短文本 → 衬线大字居中（hero）；中等文本 → 大行距正文；长文本 → 常规阅读版式；图片独立圆角块随文展示；具体观感随截图反馈迭代

## 5. 已确认的取舍

- Markdown 语法支持移除（8）；检索对象为 plain 纯文本
- 旧 Markdown 演示数据已重置（用户确认）
- 农历/节假日不含法定调休（4 采用 公历节日+农历节日+节气）
- 图片与文字关系 = 图文混排（用户选择）
- 首页架构 = 录入 / 时间轴 分开成 Tab（用户选择），且录入即为第一屏
- 整体外壳 = 桌面「左侧常驻导航 + 右侧工作区」，移动端「顶部操作条 + 底部 Tab」（调研主流笔记应用后重写）
- 时间轴视觉 = 居中单线 + 日期分组（用户选择）

## 6. Roadmap / 待打磨

- 编辑器已定为 Tiptap（单栏所见即所得富文本），复杂嵌套/表格边界待打磨；图片上传当前单张，支持多图、孤儿图片/附件清理
- 从备份恢复入口
- 法定节假日调休表（每年维护）
- **#8 网页抓取与卡片嵌入（暂缓）**：目标——链接粘贴自动识别为卡片，支持 Web 图、Apple Music、YouTube / Bilibili 视频嵌入；拟用 Tiptap 自定义节点实现（详见 README「开发计划」）

### 6.1 规划新增（未落地，先立项）

- **记录导出（图片 / PDF）**
  - 范围：单条或批量时间点 → 长图 / A4 PDF（服务于打印）
  - A4 排版要点：页边距、页眉页脚、字号、正文宽度；分页避免截断（图片/卡片/代码），嵌入组件按需包含或折叠
  - 方案候选：前端 html2canvas + jsPDF（客户端零服务负担）或服务端无头渲染保真更高；实现前先评估图文混排与嵌入卡保真
- **PopClip 快速笔记（macOS）**
  - 接入 PopClip（popclip.app/dev）：任意应用选中文本 → PopClip「记入快记」动作
  - 扩展：PopClip JS Bundle / URL 动作，选中文本 POST 到本地服务；仅本机来源 + 预留访问令牌
  - 快记侧提供极简接收接口（文本/可选标签/来源），立即生成时间点并轻提示
  - 只走纯文本链路，不涉及图片/卡片；随扩展附安装与配置说明
