# dependencies
node_modules/

# build output
web/dist/

# runtime data (SQLite DB, uploaded images, local backups, config)
server/data/

# os / editor
.DS_Store
*.log

# 一键部署脚本（本地使用，不入库）
deploy/install-arch.sh
